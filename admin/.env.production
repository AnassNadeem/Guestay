# Production env baked into the admin SPA at `vite build` time.
# Local `npm run dev` still uses localhost unless you override these.
VITE_SITE_URL=https://guestay.pk
VITE_ADMIN_URL=https://admin.guestay.pk
