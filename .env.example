# Guestay Phase 1

## Contact form webhook (optional)

Set either env var to forward enquiries to n8n or a Cloudflare Worker:

```
CONTACT_WEBHOOK_URL=https://your-n8n-or-worker-endpoint
```

Without it, `/api/contact` validates and logs the payload (dev-friendly stub).
